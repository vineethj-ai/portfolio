# External Integrations

## Analytics
- **Vercel Analytics** (`@vercel/analytics`): Used for tracking visitor metrics, likely deployed on Vercel.

## APIs & External Data
- There appear to be no external backend APIs integrated via `package.json`. Data is likely statically defined or fetched from local JSON.
- No database SDKs or auth providers are visibly present.

## Deployment
- Deployed on Vercel (inferred from the analytics package).
